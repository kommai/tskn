# SSH
ssh -l mitsumotocs -i /home/mitsumotocs/.ssh/mitsumotocs.xsrv.jp.key mitsumotocs.xsrv.jp -p 10022
63zn6rfs

# Rollup Kommai components
rollup ./public_html/vendor/tskn/kommai/src/kommai.js --file ./public_html/vendor/tskn/kommai/dist/kommai.js --format esm

ミュータブルか、イミュータブルか
どちらか一方にすべきであるという議論は無駄である。
なぜならミュータブルにせざるを得ないところ（DOMstyleとか）もあれば、イミュータブルでないと実現できないこともあるからだ。
適宜使い分けるしかない。

